[tool.ruff]
target-version = "py312"
line-length = 100
exclude = [
    ".git",
    ".venv",
    "venv",
    "notebooks",
    "results"
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = [
    "BLE001",  # Allow catching general Exception for non-blocking cache/logging
]
